import Head from 'next/head';

const HomePage = () => {
    return (
        <div className="mt-12">
            <Head>
                <title>Sách giảm giá</title>
            </Head>
        </div>
    );
};

export default HomePage;
